import { css } from "@emotion/core"

export const iframeWrapper = theme => css`
  width: 80%;
  margin: 80px auto;
  position: relative;
  z-index: 2;
`

export const dashboard = theme => css`
  border-radius: 24px;
  border: 1px solid rgba(0, 0, 0, 0.05);
`
